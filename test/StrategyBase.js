const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("StrategyBase", function () {
  it.skip("Should deploy successfully (abstract, cannot deploy)", async function () {
    // Abstract contract, cannot deploy directly
  });
});
